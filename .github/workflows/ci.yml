name: CI Pipeline

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  dummy-check:
    runs-on: ubuntu-latest

    steps:
      - name: Pass
        run: echo "CI Passed"
        